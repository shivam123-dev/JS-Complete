// Functions in Javascript

// DRY (Principal) = Do not repeat yourself
function avg(a, b) {
    // return (a + b) / 2;
    let c = (a + b) / 2;
    return c;
}

let c1 = avg(4, 6);
let c2 = avg(14, 16);
console.log(c1, c2);